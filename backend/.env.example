# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
# For local: mongodb://localhost:27017/felicity
# For Atlas: mongodb+srv://username:password@cluster.mongodb.net/felicity
MONGODB_URI=mongodb://localhost:27017/felicity

# JWT Configuration
# Secret key for signing tokens - use a long random string in production
JWT_SECRET=your_super_secret_jwt_key_change_in_production
# Token expiration time (e.g., 7d, 24h, 60m)
JWT_EXPIRE=7d

# Admin Credentials (seeded on first run)
ADMIN_EMAIL=sambhavi.singh@research.iiit.ac.in
ADMIN_PASSWORD=Aaravkhushikaly7284#

# Email Configuration (for sending tickets, notifications)
# Using Gmail: Enable "Less secure apps" or use App Password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=felicity7284@gmail.com
EMAIL_PASS=admin1234#

# Default From/Reply-To addresses. Use a no-reply sender for outbound system emails.
EMAIL_FROM="Felicity IIIT <no-reply@felicity.iiit.ac.in>"
EMAIL_REPLY_TO=no-reply@felicity.iiit.ac.in

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:5173
